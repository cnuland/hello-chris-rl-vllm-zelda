apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: dialog-route
  labels:
    app.kubernetes.io/part-of: zelda-rl
    app.kubernetes.io/name: dialog-route
spec:
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: llm-d-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /dialog
    filters:
    - type: RequestHeaderModifier
      requestHeaderModifier:
        add:
        - name: x-model-type
          value: qwen25-7b
        - name: x-cache-enabled
          value: "true"
    backendRefs:
    - group: inference.networking.x-k8s.io
      kind: InferencePool
      name: qwen25-text-pool
      port: 8000
      weight: 1
    timeouts:
      backendRequest: "30s"
      request: "45s"
