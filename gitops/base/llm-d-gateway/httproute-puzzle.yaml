apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: puzzle-route
  labels:
    app.kubernetes.io/part-of: zelda-rl
    app.kubernetes.io/name: puzzle-route
spec:
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: llm-d-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /puzzle
    filters:
    - type: RequestHeaderModifier
      requestHeaderModifier:
        add:
        - name: x-model-type
          value: qwen25-32b
        - name: x-cache-enabled
          value: "true"
    backendRefs:
    - group: inference.networking.x-k8s.io
      kind: InferencePool
      name: qwen25-puzzle-pool
      port: 8000
      weight: 1
    timeouts:
      backendRequest: "60s"
      request: "90s"
